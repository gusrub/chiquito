web: bundle exec puma -C config/puma.rb
# Uncomment the following line to enable a sidekiq queue
# if you want to enable the TITLE_PULL_QUEUE option:
# worker: bundle exec sidekiq -q default
release: rake db:migrate
